/**
 * Progress UI presentation component deleted as part of progress UI removal task.
 * Backend event infrastructure (globalKairoEventBus, MessageRouter, eventEvents) remains 100% active.
 */
export {};
